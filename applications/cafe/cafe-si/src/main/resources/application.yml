spring:
  application:
    name: cafe
  profiles:
    active: standalone

app:
  version: @project.version@

logging:
  level:
    root: ERROR

management:
  endpoints:
    web:
      exposure:
        include: "*"